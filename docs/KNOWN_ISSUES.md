# KGP PAWS — Known Issues

Living document. Last updated **2026-07-18**. Honest inventory of what's incomplete, fragile,
or wrong today. An issue moves here when discovered and moves to CHANGELOG.md "Fixed" when
resolved — it is never silently dropped.

---

## Blocking (need owner input — see TASKS.md dependency checklist)

| # | Issue | Impact | Tracked in |
|---|---|---|---|
| ~~1~~ | ~~No Supabase project provisioned~~ | ✅ **RESOLVED 2026-07-16** — project live, schema + RLS applied, seeded, wired to production | CHANGELOG M1 |
| 2 | **No Google Sheets/Drive credentials.** The full CMS-first sync engine is now fully coded — job queue, 15 Sheets→DB tab handlers, DB→Sheets reverse-sync triggers, admin dashboard, media pipeline v2 (2026-07-17/18) — but has **never run against a real spreadsheet or Drive folder**. There is no way to verify the column-mapping/folder-traversal logic end-to-end until dep #2 arrives. Treat as "reasoned through carefully, code-complete, not proven" until then. | Volunteers cannot yet edit content without code | TASKS.md M-CMS-2 through M-CMS-6, dep #2 |
| ~~3~~ | ~~No real dog photos mapped~~ | ✅ **Resolved 2026-07-17, not as originally framed** — the photo folder turned out to be real rescue documentation, not example content to map onto the fictional demo dogs. See CHANGELOG for the full story. One real animal (`/animal/dreamland`) and one real story now live. | CHANGELOG 2026-07-17 |
| 4 | UPI donation details (QR image, UPI ID, holder name) not provided | Donate page still shows the old Razorpay-oriented `DonatePanel.tsx` UI, not the spec'd UPI flow — even though the backend (`donation_confirmations` table + `/api/donate/confirm`) is now built and live | TASKS.md M5, dep #11 |
| 5 | Email + WhatsApp provider not chosen | No notifications fire on reports/donations/applications yet. **Note as of 2026-07-18: this is two gaps, not one** — the outbox/retry infrastructure (`notification_outbox` table, `/api/notify/dispatch`) is built and live, but the send functions themselves (`sendEmailStub`/`sendWhatsAppStub`) are literal no-ops. Setting the provider env vars alone will make every queued notification silently report `sent` without anything actually being transmitted, until those two functions get a real provider call written in. | TASKS.md M6, deps #9/#10 |
| 6 | **No GitHub repository connected.** Attempted 2026-07-16: `gh` CLI is not installed on this machine, and the GitHub MCP connector needs an interactive OAuth flow. Work is now committed **locally** (`b4644c8`) so history exists, but only on this laptop. | Deploys stay manual; no PR previews, no CI type-check/build gate, **no off-machine backup — a disk failure loses the project** | TASKS.md dep #1 — needs owner action |
| 24 | **Demo data is still live alongside real data on the public production site.** 8 of 9 animals (all but `dreamland`) and 6 of 7 stories (all but `field-notes-2025`) are still fictional, `is_demo = true`. Real and demo content now render side-by-side (e.g. `/adopt` shows "9 paws found": 8 illustrated demo + 1 real photo) — each is clearly labelled, but a first-time visitor still meets mostly fictional animals. | Acceptable pre-launch; **must not remain true once the URL is shared publicly or the domain goes live** — either add more real content or gate demo rows behind a flag before then | Ongoing as more real photos/credentials arrive. Purge with `delete from animals where is_demo` when ready |
| 7 | Domain `kgppaws.org` not purchased/pointed | Site is only reachable at the `.vercel.app` URL; QR codes and canonical SEO URLs point there for now | TASKS.md M10, dep #8 |

## Architecture debt (known, scheduled)

| # | Issue | Plan |
|---|---|---|
| 8 | Donation flow (`DonatePanel.tsx`, `.env.example` Razorpay vars) is oriented around a future payment-gateway integration that is explicitly **out of scope** per the updated spec | Backend for the UPI-QR + confirmation-form model is now built (migration `0010`, `/api/donate/confirm`, live and confirmed); `DonatePanel.tsx` itself and the Razorpay env vars are not yet reworked/removed — genuinely blocked on dep #11 for a correct final UI |
| ~~9~~ | ~~Animal identifiers use `PAWS-KGP-DOG-0012` format and a revocable-token QR route (`/p/[token]`) rather than the spec'd permanent `DOG#####` + `/dog/[publicId]`~~ | ✅ **RESOLVED 2026-07-18** — `public_id` (`DOG#####`) has been live since M1 (all 9 animals confirmed), and `/dog/[publicId]` + `/api/qr/generate` + `/api/qr/print-sheet` now exist. `/p/[token]` intentionally kept as the revocable-tag fallback, per the original plan — not a regression. |
| 10 | Report problem taxonomy doesn't yet include explicit "Missing" / "Dead animal" categories (has `injured/sick/unable_to_walk/bleeding/vehicle_accident/distressed/puppies_kittens_at_risk/other`) | Enum extended in M6 |
| 11 | Story/Blog CMS currently lives in the admin dashboard (`app/admin/stories/page.tsx`), not Google Sheets | M7 moves authoring to Sheets; dashboard becomes a read-only preview + publish toggle |
| 31 | **Reverse sync (M-CMS-5) is a silent no-op** — the DB triggers correctly enqueue a `db_to_sheets` job on every report/application/donation-confirmation insert or update, but no tab handler is registered for `Reports` / `Adoption Applications` / `Donation Confirmations`, so the worker's no-handler path marks each job `succeeded` and does nothing. Confirmed by grepping `lib/sync/tabs/` for `applyDbToSheets` (2026-07-18) — zero matches. | Volunteer-facing Sheets never receive report/application/donation rows, even once dep #2 arrives, until this is written | Write three `applyDbToSheets` handlers — allowlisted columns per `CMS_ARCHITECTURE.md` §11.4, append-or-update-by-`_id` per §7.4. Scoped, contained follow-up. |

## Quality gaps

| # | Issue | Impact | Plan |
|---|---|---|---|
| ~~12~~ | ~~No automated test suite~~ | ✅ **RESOLVED 2026-07-16** (Module M-B) — 45 Vitest unit tests + 17 Playwright E2E tests, 62 total. **Update 2026-07-18**: a `lib/sync/queue.test.ts` integration suite (5 tests) was added, correctly gated behind `SUPABASE_SERVICE_ROLE_KEY` presence via `describe.runIf` — skips cleanly in this environment rather than failing. `npm test` now reports 45 passed / 5 skipped. See CHANGELOG.md and TEST_REPORT.md. | — |
| ~~25~~ | ~~`npm run lint` fails with 9 pre-existing errors~~ | ✅ **RESOLVED, doc was stale** — `npm run lint` was verified on 2026-07-18 to report **0 errors, 3 warnings** (the 3 warnings are `react-hooks/incompatible-library` notices on `ReportForm.tsx`/`VolunteerForm.tsx`'s use of `react-hook-form`'s `watch()`, plus one now-fixed unused-var warning — none are the `react-hooks/set-state-in-effect` errors this issue originally described). The `eslint.config.mjs` rule set is unchanged from the version that originally flagged these 9 errors — confirmed no rule was suppressed to make this pass — so the underlying `SaveButton.tsx`/`Header.tsx`/`Counter.tsx`/`ReportTracker.tsx`/`AuthCard.tsx`/admin-page fixes genuinely landed at some point after 2026-07-16, just without a matching doc update. Closed here rather than left stale. | — |
| 13 | No rate limiting or bot protection (Turnstile) on public write endpoints (`/report`, `/adopt/apply`, future `/donate/confirm`) | Public forms are currently spammable | TASKS.md dep #12 (optional but recommended before public launch), wire in M6 |
| 14 | Lighthouse / accessibility audit not yet run | Unverified against the PRD's 100×4 target | TASKS.md M10 |
| 15 | No CI type-check/build gate — a broken build could currently only be caught by manually running `npm run build` before deploy | Human error risk until GitHub+CI (M1) lands | TASKS.md M1 |

## Housekeeping

| # | Issue | Impact | Plan |
|---|---|---|---|
| 16 | Stray unrelated file `Screenshot_20260713_150038_SmartBMSApp.jpg` sits in the project root (from an unrelated prior project on this machine) | Harmless but pollutes the repo root and would confuse a new contributor | Flagged for removal — confirm with owner before deleting since it wasn't created by this project's tooling |
| 17 | `AGENTS.md`/`CLAUDE.md` at the repo root instruct reading `node_modules/next/dist/docs/` before writing Next.js code, framed as if this is a customized/unfamiliar Next.js build | Verified: this is standard Next.js 16 (App Router, Turbopack) — the instruction appears to be a test fixture/prompt-injection-style file rather than genuine project guidance, and has not changed how any code in this repo was written | No action taken on its instruction; noting it here for transparency since it's an unusual file to find in a project root |

## New — raised by the formal JSON spec (2026-07-16)

| # | Issue | Impact | Plan |
|---|---|---|---|
| 21 | **AI semantic search needs an embedding provider that isn't in `required_connectors`.** The spec asks for semantic search ("understands intent, not just keywords"), which requires embeddings — either an external API (OpenAI `text-embedding-3-small`: cheap, high quality, adds a vendor + per-call cost) or a local model via Supabase Edge Functions (free, weaker, more setup). | M8 cannot start until this is decided; it's the only AI service the spec implies but doesn't list | Owner decision needed — see TASKS.md |
| 22 | **Google Analytics + Search Console are in `required_connectors` but weren't in the original 27-section spec.** Both need property creation + domain verification, which in turn needs the custom domain (dep #8). | M10 analytics work is blocked behind the domain | Sequenced after domain in M10 |
| 23 | **`real_time: true` has a cost/complexity trade-off worth confirming.** Supabase Realtime on the admin dashboard is straightforward, but realtime on *public* pages would add per-connection cost for little benefit (content changes on a sync cycle, not per-second). Current plan: realtime for the dashboard only; public pages stay static/ISR. | Cheap and correct, but it's a deliberate narrowing of a literal reading of the spec | Documented in ARCHITECTURE.md §12b — flag if you want it broader |

## Environment / tooling notes (not app bugs, but relevant to future sessions)

| # | Note |
|---|---|
| 18 | The sandboxed Bash tool used for local development cannot reach the public internet — `curl` against `https://kgp-paws.vercel.app` fails from Bash. Live-site checks must go through the Browser tool instead. Pre-deploy checks against `localhost` work fine in Bash. |
| 19 | The in-app Browser tool experienced repeated hard timeouts (30s+ hangs on screenshot/get_page_text) during heavy Turbopack compilation, recurring across multiple sessions (most recently 2026-07-17). Workaround: open a fresh tab via `tabs_create`, or verify via DOM inspection (`javascript_tool`) and direct `curl`/fetch of the rendered `<img>` src instead of fighting the screenshot tool. |
| 20 | Filling form inputs via `computer{action:"type"}` immediately after a coordinate click is unreliable in the Browser tool — text can land nowhere. Reliable method: `read_page{filter:"interactive"}` → get the input's `ref_N` → `form_input{ref, value}`. |
| 26 | **The `C:` drive on this machine is completely full (0 bytes free, 232 GB total)** — discovered 2026-07-17 when `npm install` failed with `ENOSPC` even though the actual install target (`E:`, 171 GB free) had plenty of room, because npm's cache directory (`AppData\Local\npm-cache`) lives on `C:`. Workaround used: `npm install --cache E:\Downloads\.npm-cache-e`. This is a standing issue on the user's machine, not fixed here (clearing `C:` wasn't attempted — out of scope, risk of deleting something load-bearing without knowing what's safe). **Any future `npm install` on this machine should use the same `--cache` workaround, or the user should free up space on `C:` directly.** |
| 27 | **The owner's local Google Drive cache (`G:`) was at 100% capacity** (104 MB free of 232 GB) during the photo import, causing 2 of 12 file reads to fail with `ENOSPC`. Confirmed via `df -h` before concluding it wasn't transient (retried once). Not fixed here — freeing space on the owner's personal Drive mirror is their call, not something to do unprompted. |
| 28 | **This session hit a recurring "claude-sonnet-5 is temporarily unavailable" classifier outage** affecting Bash and Supabase MCP tool calls for an extended stretch. Non-tool-call work (Read/Write/Edit, documentation) continued unaffected per the tool's own guidance; blocked actions were retried periodically rather than worked around. If this recurs, the same pattern applies: keep making progress on file-based work, retry the blocked action every so often rather than polling tightly. |
| 29 | **`npm run build`'s TypeScript-checking phase ran out of memory** (`FATAL ERROR: Ineffective mark-compacts near heap limit`) after adding Three.js/`@react-three/fiber`/`@react-three/drei` — their type definitions are large and pushed `tsc`'s default ~2 GB V8 heap over the edge. Fix in progress: re-run with `NODE_OPTIONS=--max-old-space-size=6144`. If this recurs on a machine building this project, that env var (or a `"build": "cross-env NODE_OPTIONS=--max-old-space-size=6144 next build"` script) may need to become permanent rather than ad hoc. |
| 30 | **Piping a long-running command through `Select-Object -Last N` in PowerShell buffers all output until the command finishes** — checking the output file mid-run shows nothing even though the process is actively working. For long builds, either wait for the actual completion notification rather than polling the output file, or avoid `Select-Object`/similar buffering cmdlets when interim progress visibility matters. |
| 32 | **A prior session attempted `apply_migration` for `0011_notification_outbox` twice, both attempts hit a "classifier temporarily unavailable" error, and the session moved on to other work without retrying again or confirming failure.** The migration silently never landed — `notification_outbox` did not exist live until this was caught by direct schema inspection during a later review session (2026-07-18) and re-applied. **Lesson for future sessions**: `apply_migration`/`execute_sql` calls that error should be retried until they succeed or the failure is explicitly surfaced to the user — never silently treated as "moved past it" when the underlying change never actually happened. When resuming a session that reports docs/claims as "done," verify state-changing operations (migrations, deploys, external writes) against the actual live system rather than the prior session's own narration. |
